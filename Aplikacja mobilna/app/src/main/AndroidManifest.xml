<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.aplikacjagorska">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA"/>

    <uses-feature android:name="android.hardware.camera" />
    <uses-feature android:name="android.hardware.camera.autofocus"/>

    <application
        android:allowBackup="true"
        android:usesCleartextTraffic="true"
        android:icon="@mipmap/ic_launcher"
        android:label="API"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AplikacjaGorska">
<!--        <activity android:name=".pomiarNachylenia.OdczytActivity"></activity>-->
        <activity android:name=".logowanie.RegisterActivity" />
        <activity android:name=".MainActivityAdmin" />
<!--        <activity android:name=".wysokosc.WysokoscActivity" />-->
        <activity android:name=".Kategoria.KategoriaActivity" />
        <activity android:name=".Kategoria.DodajKateogorieActivity" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
<!--        <meta-data-->
<!--            android:name="com.google.android.geo.API_KEY"-->
<!--            android:value="${MAPS_API_KEY}" />-->

<!--        <activity-->
<!--            android:name=".sledzTrase.MapsActivity"-->
<!--            android:label="@string/title_activity_maps" />-->
        <activity android:name=".Produkt.ProduktActivity" />
        <activity android:name=".Produkt.DodajProduktActivity" />
        <activity android:name=".Produkt.QR_ProductActivity" />
        <activity android:name=".logowanie.LoginActivity" />
<!--        <activity android:name=".logowanie.data.temp" />-->
<!--        <activity-->
<!--&lt;!&ndash;            android:name=".ui.logowanie.LoginActivity"&ndash;&gt;-->
<!--            android:label="@string/title_activity_login" />-->
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>